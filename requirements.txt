# API dependencies only — what api/, data/, analysis/, portfolio/, and
# trading/ actually import, plus uvicorn (needed to serve the app; not
# imported by the code but required by render.yaml's startCommand).
#
# Streamlit and the Streamlit app's dependencies live in
# requirements-streamlit.txt. Test-only dependencies live in
# requirements-test.txt. See README.md "Dependencies" for which file to
# install for which job.
pandas==3.0.5
yfinance==1.5.2
python-dotenv==1.2.2
anthropic==0.120.2
alpaca-py==0.43.5
fastapi==0.141.1
pydantic==2.13.4
uvicorn[standard]==0.52.1
slowapi==0.1.10
